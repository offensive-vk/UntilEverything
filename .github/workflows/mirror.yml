name: CI / Mirror

on:
  push: 
    branches: ["**"]
  workflow_dispatch:

jobs:
  sync:
     uses: offensive-vk/auto-repo-sync/.github/workflows/sync.yml@master
     with:
        target-url: "${{ secrets.TARGET_URL }}"
        target-username: "${{ secrets.TARGET_USERNAME }}"
        target-token: "${{ secrets.TARGET_TOKEN }}"
