```javascript
// This is a simple JavaScript code block
function helloWorld() {
    console.log("Hello, world!");
}

helloWorld();

// This code block uses the `react-dom` library to render a React component
import ReactDOM from 'react-dom';

const App = () => {
    return <div>Hello, world!</div>;
};

ReactDOM.render(<App />, document.getElementById('root'));

```

```python
# This is a simple Python code block
def hello_world():
    print("Hello, world!")

hello_world()

# This code block uses the `numpy` library to perform a mathematical operation
import numpy as np

array = np.array([1, 2, 3])
print(array.sum())

```